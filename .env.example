# Public site origin (no trailing slash). Build replaces __SITE_ORIGIN__ in index.html with this value.
# Netlify and Vercel usually set URL / VERCEL_URL at build time; override here if needed.
# VITE_SITE_ORIGIN=https://your-domain.com
